# ========================================================================================
# CodeChat Backend Environment Configuration
# ========================================================================================
# This file contains all environment variables needed to run CodeChat.
# Copy this file to .env and update the values with your actual credentials.
# ========================================================================================

# ========================================================================================
# NEO4J DATABASE CONFIGURATION (REQUIRED)
# ========================================================================================
# Connection URL for Neo4j graph database
# Default: neo4j://127.0.0.1:7687
# For remote Neo4j: neo4j://<host>:<port> or neo4j+s://<host>:<port> (SSL)
NEO4J_CONNECTION_URL=neo4j://127.0.0.1:7687

# Neo4j database password (REQUIRED)
# This is the password for the 'neo4j' user
# Set during Neo4j installation or via Neo4j browser
password=your_neo4j_password_here

# Neo4j database name (OPTIONAL)
# Default: neo4j
# Only change if you created a custom database in Neo4j
# Leave commented to use default
# NEO4J_DATABASE=neo4j

# ========================================================================================
# GOOGLE GEMINI API CONFIGURATION (REQUIRED)
# ========================================================================================
# Google Gemini API key for AI-powered summaries and query responses
# Get your API key at: https://makersuite.google.com/app/apikey
# Used for:
#   - Generating code summaries
#   - Processing natural language queries
#   - Generating intelligent responses with citations
GOOGLE_API_KEY=your_google_api_key_here

# Gemini AI Model (OPTIONAL)
# Default: gemini-2.0-flash-exp
# Options: gemini-2.0-flash-exp, gemini-1.5-pro, gemini-1.5-flash, gemini-2.5-flash
# This determines which Google Gemini model to use for AI responses
# Leave commented to use default
# GEMINI_MODEL=gemini-2.0-flash-exp

# ========================================================================================
# GITHUB CONFIGURATION (REQUIRED)
# ========================================================================================
# GitHub Personal Access Token for repository access
# Create token at: https://github.com/settings/tokens
# Required scopes: repo (for private repos) or public_repo (for public repos only)
# Used for:
#   - Cloning repositories
#   - Accessing repository metadata
#   - Rate limit increases (5000 requests/hour vs 60 without token)
ACCESS_TOKEN=your_github_token_here

# ========================================================================================
# CORS CONFIGURATION (OPTIONAL)
# ========================================================================================
# Comma-separated list of allowed origins for CORS
# Default: http://localhost:3000,http://127.0.0.1:3000
# Add additional origins if deploying to production or using custom ports
# Examples:
#   - Development: http://localhost:3000,http://127.0.0.1:3000
#   - Production: https://yourdomain.com,https://www.yourdomain.com
#   - Custom port: http://localhost:3001,http://localhost:3000
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# ========================================================================================
# ADDITIONAL NOTES
# ========================================================================================
# - All REQUIRED variables must be set for the application to work properly
# - OPTIONAL variables can be left commented out to use defaults
# - Never commit the actual .env file to version control
# - Update this .env.example file when adding new environment variables
# - Restart the backend server after changing environment variables
# ========================================================================================
